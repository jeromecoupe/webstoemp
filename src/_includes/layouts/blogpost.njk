{% extends "layouts/base.njk" %}
{% set activeSection = "blog" %}

{% block content %}
  <p>{{ page.date | date("MMM DD, YYYY") }}</p>
  <h1>{{ title }}</h1>
  <div>
    <p>{{ excerpt }}</p>
  </div>

  {{ content | safe }}
{% endblock %}
